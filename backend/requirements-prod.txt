fastapi
uvicorn[standard]
pydantic
python-dotenv

# Core data & ML dependencies
pandas
numpy
scikit-learn

# Prophet (optional). Railway may skip on ARM builds, fallback works.
prophet==1.1.5 ; platform_machine != "arm64"

# Async HTTP client for news_ingest
httpx

# Optional DB integrations (safe without keys)
pymongo
redis

# SSE / streaming support
aiofiles

# Optional NLTK sentiment, used safely if available
nltk

# Prometheus metrics
prometheus-client

# Date parsing
python-dateutil
